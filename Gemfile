source "https://rubygems.org"

# http_parser breaks when directories have spaces, so we'll do this to workaround
gem 'http_parser.rb', git: "https://github.com/tmm1/http_parser.rb.git"
gem "jekyll", "~> 4.2.0"
gem 'bootstrap', '~> 5.1'

# Need this if/when using Ruby 3+
gem "webrick", "~> 1.7"
